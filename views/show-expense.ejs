<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Expense Tracker</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/table.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
  <ul class="nav nav-pills">
    
    <li class="nav-item">
      <a class="nav-link " href="/">Add Expense</a>
    </li>
    <li class="nav-item">
      <a class="nav-link active" href="/show-expense">All Expense</a>
    </li>
    <li class="nav-item">
      <a class="nav-link " href="/add-money">Add in wallet</a>
    </li>
  </ul>
 <div class="container">
    <div class="totalamount">
        <h1>Total Amount</h1>
        <h1><%=total%> Rs</h1>


    </div>
  <div class="output">
    <% if (expenses.length > 0) { %>
        <ul>
          <% expenses.forEach(function(expense) { %>
            <% var remainingAmount = total - expense.amount; %>
            <li>
              
              <span id="details"><%= expense.details %>    <%= expense.amount %> RS.</span>
              <span>
                <a href="/edit/<%= expense.id %>">Edit</a>
                <a href="/delete/<%= expense.id %>">Delete</a>
              </span>
            </li>
            <div>
                <p id="remaining">Remaining Amount: <%= remainingAmount %> Rs</p>
              </div>
          <% }) %>
        </ul>
      
        
       
      <% } else { %>
        <p>No data</p>
      <% } %>
      
  </div>
</div>
  
</body>
</html>